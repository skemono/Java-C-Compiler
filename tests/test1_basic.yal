(* Test 1: Definiciones y reglas basicas *)

let digit = ['0'-'9']
let letter = ['a'-'z''A'-'Z']
let id = letter(letter|digit)*

rule tokens =
    id          { return ID }
  | digit+      { return NUMBER }
  | '+'         { return PLUS }
